{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Test to store the data",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "api-test": "newman run test/jsonplaceholder.postman_collection.json --reporters juit --reporter-junit-export newman.xml"
  },
  "author": "Rajesh",
  "dependencies": {
    "newman": "^5.3.2"
  }
}
